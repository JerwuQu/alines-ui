name: alines
description: Android remote dynamic menu

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  async: ^2.8.2
  flutter:
    sdk: flutter

  google_fonts: ^3.0.1
  shared_preferences: "^2.0.8"

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
